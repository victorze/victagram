import { NavContainer, StyledNav, Logo } from "./styles"

export const Nav = () => {
  return (
    <NavContainer>
      <StyledNav>
        <Logo href="#">
          Clontagram
        </Logo>
      </StyledNav>
    </NavContainer>
  )
}
